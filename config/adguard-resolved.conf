[Resolve]
# Keep 127.0.0.53 and /etc/resolv.conf for VPN split DNS. More-specific VPN
# route-only domains win over ~.; ordinary queries go to AdGuard on 127.0.0.1.
DNS=127.0.0.1
Domains=~.
DNSOverTLS=no
DNSSEC=no
FallbackDNS=
